---
- hosts: all
  become: true
  tasks:

  - name: Install the apache and php packages
    package:
      name: 
        - "{{ apache2_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes
